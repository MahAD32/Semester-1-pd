#include <iostream>
#include <cmath>
using namespace std;
int main(){
           int a,b,d,c,e;
           cout<<"Enter the value of a: ";
      cin>>a;
    cout<<"Enter the value of b: ";
   cin>>b;
    cout<<"Enter the value of c: ";
   
   d= (b^2)-4(a)*(c);



if d >0;
{
cout<<"Solutions: x = "<<((-b+sqrt((b^2)-4a*(c)))/(2a))<<" and x = "<<((-b-sqrt((b^2)-4a*(c)))/(2a));
}
}
